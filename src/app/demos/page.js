import DemosIndex from '@/components/POVlib/DemosIndex';

export const metadata = {
  title: 'CS2 POV Demos | POVlib',
  description: 'Browse and filter all CS2 professional and community point-of-view (POV) demos. Find top plays, map strategies, and player highlights.',
}

export default function DemosPage() {
  return <DemosIndex />;
}